---
id: icp-map
type: icp-map
version: 1
last_updated: 2026-09-17
---

ICP Map — accumulated name → ICP-category mappings confirmed across
`/monitor-engagement audience` runs (Phase 24, REQUIREMENTS.md §35), so a
repeat account isn't re-classified from scratch every time it shows up in
a new post's likers/commenters list. Same system-appended living-doc
pattern as `hook-formulas.md`/`humanizer-rules.md`/`algorithm-rules.md`,
but entries here are appended only after the user explicitly confirms a
classification during a real run — never inferred and written silently,
and never seeded with invented names.

## Mappings

| name | category | last_confirmed_date |
|---|---|---|
